[package]
name = "mlp-cuda-kernels"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[[bin]]
name = "mlp-cuda-kernels"
path = "src/main.rs"

[dependencies]
mlp-common = { path = "../mlp-common" }

[build-dependencies]
cc = { version = "1.0", features = ["parallel"] }
