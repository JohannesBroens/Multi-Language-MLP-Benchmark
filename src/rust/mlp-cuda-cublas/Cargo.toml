[package]
name = "mlp-cuda-cublas"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "mlp-cuda-cublas"
path = "src/main.rs"

[dependencies]
nn-common = { path = "../nn-common" }

[build-dependencies]
cc = { version = "1.0", features = ["parallel"] }
